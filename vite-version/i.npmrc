# Use a flat node_modules like npm/yarn classic
node-linker=hoisted

# (Optional, but helps ensure the rollup stuff is reachable at the top)
public-hoist-pattern[]=rollup
public-hoist-pattern[]=@rollup/*